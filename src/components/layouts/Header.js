//React
import React, {PropTypes} from 'react';
import {} from 'prop-types';
//Component

const Header = (props) => {
    return (
        <div>
            header
        </div>
    );
};

Header.PropTypes = {};

export default Header