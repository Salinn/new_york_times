//React
import React, {PropTypes} from 'react';
import {} from 'prop-types';
//Component

const Footer = (props) => {
    return (
        <footer>
            footer
        </footer>
    );
};

Footer.PropTypes = {};

export default Footer